let nombre = 'Hola Mundo';
let NombreCompleto;  /*UpperCamelCase*/
let nombreCompleto; /*camelCase*/
let nombre_completo; /*snake_case*/
console.log(nombre);

let apellido; /* se debe indicar solamente una vez, no se puede volver a declarar */
apellido = 'Ochoa'; /* se puede definir mas adelante */
let animal;

let genero, edad; /* no se recomienda declarar varias variables en una sola linea */
/*cada variable debe ser declarada en su propia linea */

/* Nunca se debe colocar*/ x, a, n1, abc, r, r1 /*como nombres de variables
   Siempre se deben usar nombres significativos es decir indicar el valor que esta va a tener */