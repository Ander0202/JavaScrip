const titulo = document.getElementById('titulo');

console.log(titulo.innerText); //innerText muestra el texto

titulo.innerText = 'Mis toppings Favoritos'; //modificar texto del DOM con JS

console.log(titulo.innerText);

